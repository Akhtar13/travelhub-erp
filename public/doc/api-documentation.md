# API Documentation

Authenticate with `POST /api/auth/login` using email/password, then send `Authorization: Bearer <token>`.

Endpoints: `GET/POST /api/bookings`, `GET /api/bookings/{booking}`, `GET /api/routes`, `GET /api/agents`, `GET /api/passengers`, and `GET /api/reports/{bookings|passengers|agents|credits|revenue}`.
