@extends('admin.layouts.master') @section('content')<h1>{{ $layout ? 'Edit' : 'Create' }} Seat Layout</h1><form method="post" action="{{ $layout ? route('admin.seat-layouts.update',$layout) : route('admin.seat-layouts.store') }}">@csrf @if($layout)@method('PUT')@endif<input name="name" value="{{ old('name',$layout?->name) }}" placeholder="Name"><input type="number" name="rows" value="{{ old('rows',$layout?->rows ?? 10) }}"><input type="number" name="columns" value="{{ old('columns',$layout?->columns ?? 4) }}"><select name="status"><option value="active">Active</option><option value="inActive" @selected(old('status',$layout?->status)==='inActive')>Inactive</option></select><p>Blocked seat numbers, e.g. A1, A2.</p><input name="blocked_seats[]" placeholder="Blocked seat"><input name="blocked_seats[]" placeholder="Blocked seat"><button>Save</button></form>@endsection
