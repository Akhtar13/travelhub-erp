@extends('admin.layouts.master') @section('content')<h1>Agent Wallet</h1>@foreach($agents as $agent)<form method="post" action="{{ route('admin.wallet.credit',$agent) }}">@csrf <strong>{{ $agent->name }}:</strong> {{ $agent->credit_balance }} <input name="amount" placeholder="Amount"><input name="description" placeholder="Description"><button>Credit</button></form><form method="post" action="{{ route('admin.wallet.debit',$agent) }}">@csrf <input name="amount" placeholder="Amount"><input name="description" placeholder="Description"><button>Debit</button></form>@endforeach<h2>History</h2><table>@foreach($transactions as $t)<tr><td>{{ $t->created_at }}</td><td>{{ $t->agent->name }}</td><td>{{ $t->type }}</td><td>{{ $t->amount }}</td><td>{{ $t->balance_after }}</td></tr>@endforeach</table>@endsection
