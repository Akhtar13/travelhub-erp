@extends('admin.layouts.master') @section('content')<h1>{{ ucfirst($type) }} Report</h1><form><input type="date" name="from" value="{{ request('from') }}"><input type="date" name="to" value="{{ request('to') }}"><button>Filter</button></form><a href="{{ route('admin.reports.export',[$type,'csv']) }}">Excel CSV</a> <a href="{{ route('admin.reports.export',[$type,'pdf']) }}">PDF</a><pre>{{ print_r(method_exists($rows,'items')?$rows->items():$rows->toArray(),true) }}</pre>@if(method_exists($rows,'links')){{ $rows->links() }}@endif@endsection
