@extends('admin.layouts.master') @section('content')<h1>Travel Schedules</h1><a href="{{ route('admin.travel-schedules.create') }}">Create Schedule</a><table>@foreach($schedules as $schedule)<tr><td>{{ $schedule->route->name }}</td><td>{{ $schedule->departure_at }}</td><td>{{ $schedule->seatLayout->name }}</td><td>{{ $schedule->base_fare }}</td><td>{{ $schedule->status }}</td><td><a href="{{ route('admin.travel-schedules.edit',$schedule) }}">Edit</a><form method="post" action="{{ route('admin.travel-schedules.destroy',$schedule) }}">@csrf @method('DELETE')<button>Delete</button></form></td></tr>@endforeach</table>{{ $schedules->links() }}@endsection
