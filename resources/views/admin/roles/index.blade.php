@extends('admin.layouts.master') @section('content')<h1>Roles</h1><a href="{{ route('admin.roles.create') }}">Create</a>@foreach($roles as $role)<p>{{ $role->name }} <a href="{{ route('admin.roles.edit',$role) }}">Edit</a><form method="post" action="{{ route('admin.roles.destroy',$role) }}">@csrf @method('DELETE')<button>Delete</button></form></p>@endforeach{{ $roles->links() }}@endsection
