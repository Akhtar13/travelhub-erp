@extends('admin.layouts.master') @section('content')<form method="post" action="{{ $role->exists ? route('admin.roles.update',$role) : route('admin.roles.store') }}">@csrf @if($role->exists)@method('PUT')@endif<input name="name" value="{{ old('name',$role->name) }}">@foreach($permissions as $p)<label><input type="checkbox" name="permissions[]" value="{{ $p->id }}" @checked($role->exists && $role->permissions->contains($p))>{{ $p->name }}</label>@endforeach<button>Save</button></form>@endsection
