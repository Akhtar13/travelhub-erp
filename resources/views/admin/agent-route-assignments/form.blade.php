@extends('admin.layouts.master') @section('content')<h1>Assign Route</h1><form method="post" action="{{ route('admin.agent-route-assignments.store') }}">@csrf <select name="agent_id">@foreach($agents as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select><select name="travel_route_id">@foreach($routes as $r)<option value="{{ $r->id }}">{{ $r->name }}</option>@endforeach</select><input name="seat_quota" placeholder="Seat quota"><input name="daily_limit" placeholder="Daily limit"><input name="price" placeholder="Pricing"><select name="status"><option value="active">Active</option><option value="inactive">Inactive</option></select><button>Save</button></form>@endsection
