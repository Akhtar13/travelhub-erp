@extends('admin.layouts.master') @section('content')<h1>Allocate Seats</h1><form method="post" action="{{ route('admin.seat-allocations.store') }}">@csrf <select name="agent_id">@foreach($agents as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select><select name="travel_schedule_id">@foreach($schedules as $s)<option value="{{ $s->id }}">{{ $s->route->name }} {{ $s->departure_at }}</option>@endforeach</select><input name="allocated_seats" placeholder="Seats"><select name="status"><option value="active">Active</option><option value="inactive">Inactive</option></select><button>Save</button></form>@endsection
