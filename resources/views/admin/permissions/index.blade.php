@extends('admin.layouts.master') @section('content')<h1>Permissions</h1><a href="{{ route('admin.permissions.create') }}">Create</a>@foreach($permissions as $permission)<p>{{ $permission->name }} <a href="{{ route('admin.permissions.edit',$permission) }}">Edit</a><form method="post" action="{{ route('admin.permissions.destroy',$permission) }}">@csrf @method('DELETE')<button>Delete</button></form></p>@endforeach{{ $permissions->links() }}@endsection
