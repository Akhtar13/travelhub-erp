@extends('admin.layouts.master') @section('content')<h1>Global Search</h1><form><input name="q" value="{{ $q }}" placeholder="Search PNR, passenger, agent"><button>Search</button></form><h2>Bookings</h2>@foreach($bookings as $b)<p>{{ $b->pnr }} {{ $b->passenger?->first_name }}</p>@endforeach<h2>Passengers</h2>@foreach($passengers as $p)<p>{{ $p->first_name }} {{ $p->last_name }}</p>@endforeach<h2>Agents</h2>@foreach($agents as $a)<p>{{ $a->name }} {{ $a->company }}</p>@endforeach@endsection
