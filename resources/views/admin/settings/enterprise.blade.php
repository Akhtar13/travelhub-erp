@extends('admin.layouts.master') @section('content')<h1>Enterprise Settings</h1><form method="post" action="{{ route('admin.enterprise-settings.update') }}">@csrf @method('put')<input name="company_name" value="{{ data_get($settings,'company.company_name','TravelHub') }}" placeholder="Company"><input name="smtp_host" value="{{ data_get($settings,'smtp.smtp_host') }}" placeholder="SMTP host"><input name="smtp_port" value="{{ data_get($settings,'smtp.smtp_port') }}" placeholder="SMTP port"><input name="currency" value="{{ data_get($settings,'booking.currency','USD') }}" placeholder="Currency"><input name="timezone" value="{{ data_get($settings,'booking.timezone','UTC') }}" placeholder="Timezone"><input name="seat_hold_duration" value="{{ data_get($settings,'booking.seat_hold_duration',15) }}" placeholder="Seat hold minutes"><button>Save</button></form>@endsection
