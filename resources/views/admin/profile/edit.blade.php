@extends('admin.layouts.master') @section('content')<form method="post" action="{{ route('admin.profile.update') }}">@csrf @method('PUT')<input name="name" value="{{ $user->name }}"><input name="email" value="{{ $user->email }}"><input name="phone" value="{{ $user->phone }}"><button>Save Profile</button></form><form method="post" action="{{ route('admin.profile.password') }}">@csrf @method('PUT')<input name="current_password" type="password"><input name="password" type="password"><input name="password_confirmation" type="password"><button>Change Password</button></form>@endsection
