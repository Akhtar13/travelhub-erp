@extends('admin.layouts.master') @section('content')<h1>{{ $agent->exists?'Edit':'Create' }} Agent</h1><form method="post" action="{{ $agent->exists?route('admin.agents.update',$agent):route('admin.agents.store') }}">@csrf @if($agent->exists)@method('put')@endif <input name="name" value="{{ old('name',$agent->name) }}" placeholder="Name"><input name="company" value="{{ old('company',$agent->company) }}" placeholder="Company"><input name="credit_balance" value="{{ old('credit_balance',$agent->credit_balance ?? 0) }}" placeholder="Credit Balance"><select name="currency_id"><option value="">Currency</option>@foreach($currencies as $currency)<option value="{{ $currency->id }}" @selected(old('currency_id',$agent->currency_id)==$currency->id)>{{ $currency->code }} {{ $currency->name }}</option>@endforeach</select><input name="contact" value="{{ old('contact',$agent->contact) }}" placeholder="Contact"><input name="email" value="{{ old('email',$agent->email) }}" placeholder="Email"><select name="status"><option value="active">Active</option><option value="inactive">Inactive</option><option value="suspended">Suspended</option></select><button>Save</button></form>@endsection
