@extends('admin.layouts.master') @section('content')<h1>Users</h1><a href="{{ route('admin.users.create') }}">Create</a><form><input name="search" value="{{ request('search') }}"><select name="status"><option value="">All</option><option value="active">Active</option><option value="inActive">Inactive</option></select><button>Filter</button></form><table><tr><th>Name</th><th>Email</th><th>Phone</th><th>Status</th><th></th></tr>@foreach($users as $user)<tr><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ $user->phone }}</td><td>{{ $user->status }}</td><td><a href="{{ route('admin.users.edit',$user) }}">Edit</a><form method="post" action="{{ route('admin.users.destroy',$user) }}">@csrf @method('DELETE')<button>Delete</button></form></td></tr>@endforeach</table>{{ $users->links() }}@endsection
