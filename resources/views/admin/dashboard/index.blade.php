@extends('admin.layouts.master') @section('content')<h1>Dashboard</h1><div>@foreach($cards as $label=>$value)<section><strong>{{ $label }}</strong>: {{ $value }}</section>@endforeach</div><h2>Monthly Bookings</h2><pre>{{ json_encode($monthlyBookings) }}</pre><h2>Revenue</h2><pre>{{ json_encode($revenue) }}</pre><h2>Recent Activities</h2><ul>@foreach($activities as $a)<li>{{ $a->created_at }} {{ $a->description }}</li>@endforeach</ul>@endsection
