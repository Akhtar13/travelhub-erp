@extends('admin.layouts.master') @section('content')<h1>Create Booking</h1><form method="post" action="{{ route('admin.bookings.store') }}">@csrf<select name="travel_schedule_id"><option value="">Schedule</option>@foreach($schedules as $schedule)<option value="{{ $schedule->id }}">{{ $schedule->route->name }} - {{ $schedule->departure_at }}</option>@endforeach</select><select name="return_schedule_id"><option value="">No return</option>@foreach($schedules as $schedule)<option value="{{ $schedule->id }}">{{ $schedule->route->name }} - {{ $schedule->departure_at }}</option>@endforeach</select><input name="seat_ids[]" placeholder="Seat ID"><input name="seat_ids[]" placeholder="Seat ID"><input name="hold_token" placeholder="Hold token"><h3>Passenger Details</h3><input name="first_name" placeholder="First name"><input name="last_name" placeholder="Last name"><select name="gender"><option value="male">Male</option><option value="female">Female</option><option value="other">Other</option></select><input type="date" name="dob"><select name="nationality_id"><option value="">Nationality</option>@foreach($nationalities as $nationality)<option value="{{ $nationality->id }}">{{ $nationality->name }}</option>@endforeach</select><input name="passport" placeholder="Passport"><input name="national_id" placeholder="National ID"><input name="contact_number" placeholder="Contact number"><input type="date" name="return_date"><button>Confirm Booking</button></form><p>Use <code>/admin/bookings/schedules/{schedule}/seats</code> to view availability and <code>/admin/bookings/hold</code> to create temporary holds.</p>@endsection
