@extends('admin.layouts.master') @section('content')<form method="post" action="{{ $item ? route('admin.master-data.update',[$type,$item->id]) : route('admin.master-data.store',$type) }}">@csrf @if($item)@method('PUT')@endif @if(in_array($type,['branches','locations']))<select name="region_id"><option value="">No region</option>@foreach($regions as $region)<option value="{{ $region->id }}" @selected(old('region_id',$item?->region_id)==$region->id)>{{ $region->name }}</option>@endforeach</select>@endif<input name="name" value="{{ old('name',$item?->name) }}" placeholder="Name"><input name="code" value="{{ old('code',$item?->code) }}" placeholder="Code">@if($type==='branches')<input name="email" value="{{ old('email',$item?->email) }}"><input name="phone" value="{{ old('phone',$item?->phone) }}"><textarea name="address">{{ old('address',$item?->address) }}</textarea>@endif<select name="status"><option value="active">Active</option><option value="inActive">Inactive</option></select><button>Save</button></form>@endsection
